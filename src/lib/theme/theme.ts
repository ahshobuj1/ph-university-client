import type {ThemeConfig} from 'antd';

export const theme: ThemeConfig = {
  components: {
    Pagination: {
      // itemActiveBg: '#001529',
    },
    Button: {
      colorPrimary: '#001529',
    },
  },
};
